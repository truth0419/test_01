# test_01
ms 강의 중 스네이크 게임 코드 by html

***

```markdown
# 🟢 Snake Game (HTML/JS)

키보드 **방향키**로 플레이하는 스네이크 게임입니다.  
브라우저에서 바로 실행되며 점수/최고점 기록, 벽 충돌/벽 통과 모드를 지원합니다.

---

## ✨ 주요 기능

- ⌨️ **키보드 방향키 조작**
- 🧮 **점수 / 최고점(localStorage 저장)**
- 🍎 먹이를 먹으면 **길이 증가 + 속도 증가**
- 🧱 **벽 충돌** / **벽 통과(wrap)** 모드
- ⏸ 일시정지 / ⏯ 재개 / ↺ 재시작
- 🎨 Canvas 기반 부드러운 렌더링

---

## ▶️ 실행 방법

### 1) 로컬에서 실행
1. `snake.html` 다운로드 또는 생성
2. 브라우저로 열기(Chrome/Edge 추천)

### 2) GitHub Pages 배포
1. 리포지토리를 GitHub에 업로드  
2. `Settings` → `Pages`  
3. Source: **main / root** 선택  
4. 자동 생성된 URL로 접속

---

## 🎮 조작법

| 조작 | 기능 |
|------|-------|
| ↑ ↓ ← → | 이동 |
| Space | 일시정지 / 재개 |
| R | 재시작 |
| W | 벽 모드 전환 |

---

## 🗂️ 파일 구조

```

/
├── snake.html
├── README.md
└── assets/
└── screenshot.png (선택)

````

---

## ⚙️ 주요 설정값 (snake.html 내부)

```js
const CELL = 20;          // 칸 크기(px)
const SIZE = 400;         // 캔버스 크기
const START_LEN = 4;      // 시작 길이
const START_SPEED = 130;  // 낮을수록 빠름(ms)
const SPEED_STEP = 3;     // 먹이 먹을 때 빨라지는 정도
const MIN_SPEED = 60;     // 최대 속도
````

난이도 조절:

*   **쉬움** → START\_SPEED ↑ (180~~220), MIN\_SPEED ↑ (80~~100)
*   **어려움** → START\_SPEED ↓ (100), MIN\_SPEED ↓ (50\~60)

***

## 🐛 버그 수정 사항

*   **게임 시작 시 바로 종료되던 문제 해결**
    *   초기 뱀 위치가 반대 방향으로 배치되어 첫 이동에서 **자기 충돌** 발생
    *   해결: 뱀을 `push()`로 꼬리 → 머리 순서로 구성해 **머리가 배열 마지막 요소**가 되도록 수정

***

## 🧩 핵심 코드 (초기 뱀 배치)

```js
// 꼬리 → 머리 (머리가 오른쪽 끝)
for (let i = START_LEN - 1; i >= 0; i--) {
  snake.push({ x: startX - i, y: startY });
}

// 이동 시 머리 = snake[snake.length - 1]
const head = { ...snake[snake.length - 1] };
head.x += dir.x;
head.y += dir.y;
```

***

## 🤝 Contributing

이슈 또는 개선 제안은 환영합니다!  
코드 스타일은 기본 JavaScript(세미콜론 사용) 기준입니다.

***

## 📜 License

**MIT License**
